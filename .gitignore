# Environment
.env
.env.local
.env*.local

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build outputs
.next/
out/
build/
dist/

# Next.js / TypeScript generated
next-env.d.ts
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Firebase
.firebase/
.firebaserc

# API keys & secrets
credentials.json
service-account-key.json
API_KEYS.txt

# Logs
logs/
*.log

# OS
Thumbs.db
.DS_Store

# Claude Code — ignore personal settings and re-clonable third-party design
# skills, but keep the project's own agent roster and slash commands in version
# control. They are part of how this project is built.
.claude/*
!.claude/agents/
!.claude/commands/

# Windows reserved device names captured as real files. Git Bash treats `> nul`
# as a redirect to a file called `nul` rather than the null device, and git then
# cannot index it ("short read while indexing nul"), which blocks every commit.
nul
NUL

# Agent skills installed from third-party repos (Neon's, etc). The vendored
# copies are re-installable from skills-lock.json, which IS tracked — same
# reasoning as package-lock.json. .neon is local CLI state.
.agents/
.neon

# Spike inputs/outputs: course material and simulation runs. Kept out of git — the
# deck is the professor's, and the results belong in docs/ once summarised.
spike-data/

# Course material dropped in the repo root. Decks are the professor's and must not be
# committed -- same reason spike-data/ is ignored. Research papers under docs/literature/
# stay tracked, which is why this rule is anchored to the root only.
/*.pdf
