# Copy to .env.local and fill in to enable Supabase event logging.
# WITHOUT these, the app still runs — logEvent() falls back to console.info.
# (.gitignore already ignores .env*.local)

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
